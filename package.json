{
  "name": "cryptopals-ts",
  "version": "0.0.1",
  "main": "lib/index.js",
  "scripts": {
    "compile": "tsc",
    "lint": "standard",
    "prepublishOnly": "npm run compile",
    "test": "npm run compile && nyc ava lib/**/*.test.js"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "nyc": "^13.0.1",
    "standard": "^12.0.1",
    "typescript": "^3.0.3"
  }
}
